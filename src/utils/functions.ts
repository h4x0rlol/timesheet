export const getTimeModeFromStore = (store) => {
  return store.timeMode;
};

export const getDateFromStore = (store) => {
  return store.date;
};

export const getModeFromStore = (store) => {
  return store.mode;
};
